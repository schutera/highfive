# HighFive Production Environment Variables
# Copy this file to .env and fill in your production values
# ⚠️ NEVER commit the .env file to version control!

# Backend API Key
# Generate a secure key: openssl rand -base64 32
HIGHFIVE_API_KEY=your_secure_production_key_here

# Frontend API Key (usually same as backend, but can be different)
# This is built into the frontend during Docker build
VITE_API_KEY=your_secure_production_key_here

# API URL (used by frontend)
# Point to your production API domain
VITE_API_URL=https://api.highfive.schutera.com

# Node environment
NODE_ENV=production

# Backend port (internal)
PORT=3001
