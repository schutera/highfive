Flask==3.1.2
requests==2.32.5
pydantic==2.12.5
# Hole detection (#165): headless OpenCV (no GUI/X11 deps) for HoughCircles +
# crop, numpy for the brightness/texture heuristic. headless keeps the image
# slim and avoids libGL in the container.
opencv-python-headless==4.10.0.84
numpy==1.26.4
