{
  "name": "highfive-monorepo",
  "private": true,
  "workspaces": [
    "contracts",
    "backend",
    "homepage"
  ],
  "scripts": {
    "prepare": "husky || true",
    "build": "npm run build -w @highfive/contracts && npm run build -w highfive-backend && npm run build -w homepage"
  },
  "dependencies": {
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "prettier": "^3.8.3"
  }
}
