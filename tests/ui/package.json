{
  "name": "highfive-ui-tests",
  "private": true,
  "version": "0.0.0",
  "description": "Playwright UI tests for the HiveHive homepage. Not a root npm workspace - lives outside the workspaces[] array on purpose so the Playwright browser cache and runner deps stay out of homepage/backend/contracts node_modules resolution.",
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@highfive/contracts": "file:../../contracts",
    "@playwright/test": "1.56.0",
    "@types/node": "^22.10.5"
  }
}
